apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-quota-demo
  namespace: quota-object-example
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
